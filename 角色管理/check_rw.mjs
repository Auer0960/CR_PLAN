import * as ReactWindow from 'react-window';
console.log('Keys:', Object.keys(ReactWindow));
try {
    console.log('FixedSizeGrid:', ReactWindow.FixedSizeGrid);
} catch (e) { }
try {
    console.log('Grid:', ReactWindow.Grid);
} catch (e) { }
